server:
  # Recursive resolver (unbound is recursive by default)
  # No explicit 'recursion' directive needed

  # Root hints / trust anchor handled by package
  auto-trust-anchor-file: "/var/lib/unbound/root.key"

  # Prefetch helps latency for hot domains
  prefetch: yes
  prefetch-key: yes
